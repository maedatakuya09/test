# claude-kintone-template 環境構築マニュアル

対象読者:

- kintone開発者
- Git / Node.js などの開発環境にまだ慣れていないメンバー

このガイドの手順をすべて実施すると、**カスタマイズの開発・ビルド・アップロードができる状態**になります。

---

## 目次

1. [claude-kintone-template とは何か](#1-claude-kintone-template-とは何か)
2. [環境構築手順](#2-環境構築手順)
   - 2.1 [事前に必要なソフトをインストールする](#21-事前に必要なソフトをインストールする)
   - 2.2 [リポジトリを手元に取得する](#22-リポジトリを手元に取得する)
   - 2.3 [パッケージをインストールする](#23-パッケージをインストールする)
   - 2.4 [kintone の認証情報を設定する](#24-kintone-の認証情報を設定する)
   - 2.5 [アプリの設定ファイルを作る](#25-アプリの設定ファイルを作る)
   - 2.6 [実際にビルドしてみる](#26-実際にビルドしてみる)
   - 2.7 [(任意) 開発環境にアップロードして動作確認する](#27-任意-開発環境にアップロードして動作確認する)
   - 2.8 [うまくいかないときは](#28-うまくいかないときは)
3. [プラグイン開発](#3-プラグイン開発)
   - 3.1 [カスタマイズ開発との違い](#31-カスタマイズ開発との違い)
   - 3.2 [ディレクトリ構成](#32-ディレクトリ構成)
   - 3.3 [ビルド・アップロード](#33-ビルドアップロード)
   - 3.4 [コンパイルレベル(最適化)の指定](#34-コンパイルレベル最適化の指定)
   - 3.5 [秘密鍵 `private.ppk` の扱い(重要)](#35-秘密鍵-privateppk-の扱い重要)
4. [MCP連携(Claude Codeからkintoneを直接操作する)](#4-mcp連携claude-codeからkintoneを直接操作する)
   - 4.1 [MCPとは](#41-mcpとは)
   - 4.2 [セットアップ手順](#42-セットアップ手順)
   - 4.3 [使える MCP サーバー](#43-使える-mcp-サーバー)
   - 4.4 [利用時の注意点(重要)](#44-利用時の注意点重要)
   - 4.5 [対話例](#45-対話例)
5. [他に参考にした方がよいファイル](#5-他に参考にした方がよいファイル)

---

## 1. claude-kintone-templateとは

### 概要

`claude-kintone-template` は、**kintoneのカスタマイズ / プラグイン開発**に特化した、**Claude Code専用**の開発テンプレートです。TypeScript + esbuildをベースに構築されています。

> 💡 補足: 「Claude Code」はAnthropicのAIコーディングツールです。このテンプレートは、AIにkintone開発のルールやガードレールを注入した状態で開発を進められるように設計されています。

### できること

| 機能                | ひとことで言うと                                                                   |
| ------------------- | ---------------------------------------------------------------------------------- |
| カスタマイズ開発    | アプリごとの JS/CSS を書いて、kintone に自動アップロードできる                     |
| プラグイン開発      | 配布用のプラグイン(zip)を作成・署名・アップロードできる                            |
| 型定義の自動生成    | kintone のフィールド情報から TypeScript の型を自動で作ってくれる(入力ミスを防げる) |
| AI(Claude Code)連携 | ソースコードの規約や、うっかり事故を防ぐガードレールがあらかじめ用意されている     |

### 覚えておきたい用語(初心者向け)

- **Git / リポジトリ**: ソースコードの変更履歴を管理する仕組み。「リポジトリ」はコード一式が入ったフォルダのようなものです。
- **clone(クローン)**: リポジトリをインターネット上(GitHub など)から自分の PC にコピーしてくること。
- **Node.js / npm**: JavaScript のプログラムを実行するための土台(Node.js)と、それに付属するパッケージ管理ツール(npm)。`npm install` で必要な部品を一括ダウンロードします。
- **ビルド(build)**: 自分が書いたソースコードを、kintone が読み込める形式のファイルに変換する作業。
- **.env ファイル**: パスワードなどの秘密情報を書いておくための設定ファイル。Git の管理対象から除外されており、誤って社外に公開されない仕組みになっています。

このマニュアルでは、上記の用語がわからなくても迷わず進められるよう、手順を1つずつ説明します。

---

## 2. 環境構築手順

### 2.1 事前に必要なソフトをインストールする

作業を始める前に、以下の3つを PC にインストールしてください。

| ソフト                       | 用途                                 | 入手先                                              |
| ---------------------------- | ------------------------------------ | --------------------------------------------------- |
| **Git**                      | リポジトリを取得・管理するために必要 | https://git-scm.com/downloads                       |
| **Node.js**(LTS版)           | ビルドやコマンド実行に必要           | https://nodejs.org/ (「LTS」と書かれている方を選ぶ) |
| **Visual Studio Code**(推奨) | ソースコードを編集するエディタ       | https://code.visualstudio.com/                      |

インストール後、ターミナル(Windows は「コマンドプロンプト」または「PowerShell」、Mac は「ターミナル」)を開いて、以下のコマンドでインストールできているか確認します。

```bash
git --version
node --version
npm --version
```

それぞれバージョン番号が表示されれば OK です。何も表示されない/エラーになる場合は、インストールがうまくいっていないので、PC を再起動してから再度確認してください。

> **補足**: Claude Code を使って AI と対話しながら開発したい場合は、別途 Claude Code のインストールが必要です。導入手順は社内の別ドキュメントを参照するか、担当者に確認してください。このマニュアルでは Claude Code を使わない場合でも最後までビルドできる手順を説明します。

---

### 2.2 リポジトリを手元に取得する

作業用のフォルダを決め、ターミナルでそのフォルダに移動してから、以下を実行します。

```bash
git clone http://192.168.0.154/advanced-tech/kintone-dev-kit
cd <クローンされたフォルダ名>
```

これで、README.md や CLAUDE.md などのファイルが入ったフォルダが手元にできます。

---

### 2.3 パッケージをインストールする

リポジトリのフォルダの中で、以下を実行します。

```bash
npm install
```

`package.json` に書かれた必要な部品(esbuild, TypeScript など)が自動でダウンロードされます。数十秒〜数分かかることがあります。エラーが出ずに完了すれば成功です。

---

### 2.4 kintone の認証情報を設定する

ビルドしたカスタマイズを kintone にアップロードしたり、フィールド情報を取得したりするために、kintone への接続情報(URL・ユーザー名・パスワードなど)を設定します。

#### 手順

1. リポジトリ直下にある `.env.example` を `.env` という名前でコピーします。

   ```bash
   cp .env.example .env
   ```

   (Windows でコマンドがうまく動かない場合は、エクスプローラーで `.env.example` をコピー&貼り付けし、ファイル名を `.env` に変更してください。)

2. `.env` を VS Code などのエディタで開き、以下のように自分の kintone 環境の情報を書き込みます。

   ```env
   KINTONE_BASE_URL=https://your-subdomain.cybozu.com
   KINTONE_USERNAME=your-username
   KINTONE_PASSWORD=your-password
   ```

   値は担当者・チームリーダーに確認してください(開発環境用のアカウントを使うのが基本です)。

   > **重要**: `.env` は Git の管理対象外(`.gitignore` 設定済み)になっているため、ここに書いた内容が誤って社外に公開されることはありません。逆に言うと、このファイルは各自の PC ごとに個別に作成する必要があります(リポジトリに最初から入っているわけではありません)。

   > 個人検証レベルであれば、上記のように `.env` に直接書くだけで問題ありません。認証情報をリポジトリのフォルダから完全に切り離しておきたい場合は、次の「認証情報をリポジトリの外に置く方法」を参照してください。

3. 正しく設定できたか確認します。

   ```bash
   npm run check:env
   ```

   `✓ 認証情報の読み込みに成功しました` と表示されれば成功です。エラーが出る場合は `.env` の内容(スペルミスや余分な空白など)を見直してください。

#### 認証情報をリポジトリの外に置く方法(推奨)

`.env` はすでに Git の管理対象外になっているため、直接パスワードを書いても社外に流出する心配は基本的にありません。ただし、「PC 内であってもリポジトリのフォルダの中にパスワードの実物を置きたくない」という場合は、以下の方法で認証情報そのものをリポジトリの外に隔離できます。

1. PC 内の、リポジトリとは別の場所に秘密情報用のフォルダを作り、そこに実際の認証情報を書いたファイルを作成します。フォルダ名・ファイル名は自由ですが、例として以下のような場所を使います。
   - Windows: `C:\Users\<自分のユーザー名>\secrets\kintone-<プロジェクト名>.env`
   - Mac: `~/secrets/kintone-<プロジェクト名>.env`

   このファイルの中身は、通常の `.env` と同じ形式で書きます。

   ```env
   KINTONE_BASE_URL=https://your-subdomain.cybozu.com
   KINTONE_USERNAME=your-username
   KINTONE_PASSWORD=your-password
   ```

2. リポジトリ直下の `.env`(`.env.example` をコピーして作ったもの)には、値を直接書く代わりに、上記1で作ったファイルへの「パス(ファイルの場所)」だけを1行で書きます。

   ```env
   # <リポジトリのフォルダ>/.env
   ENV_FILE_REDIRECT=C:\Users\<自分のユーザー名>\secrets\kintone-<プロジェクト名>.env
   ```

   Mac の場合は例えば以下のようになります。

   ```env
   ENV_FILE_REDIRECT=/Users/<自分のユーザー名>/secrets/kintone-<プロジェクト名>.env
   ```

   > 相対パス(`../secrets/...` のような書き方)を使う場合は、`.env` が置かれているフォルダを基準に解決されます。慣れないうちは、上記のようにフルパス(絶対パス)で書く方が確実です。

3. この状態で `npm run check:env` を実行すると、リポジトリ内の `.env` ではなく、リダイレクト先の外部ファイルが読み込まれ、「ソース」の欄に外部ファイルのパスが表示されます。これで正しくリダイレクトされていることが確認できます。

   ```bash
   npm run check:env
   ```

> **この方法のメリット**: リポジトリのフォルダを誰かと共有したり、フォルダごと zip にして送ったりしても、パスワードの実物が含まれない状態になります。チームで統一ルールがある場合は、そちらの手順に従ってください。

---

### 2.5 アプリの設定ファイルを作る

カスタマイズをどの kintone アプリに反映するかを指定するファイルを作成します。

```bash
cp customize-manifest.example.json customize-manifest.json
```

作成した `customize-manifest.json` をエディタで開き、`"app"` の値を、実際に対象とする kintone アプリの ID に書き換えます(アプリ ID は kintone のアプリ画面の URL に含まれる数字です。わからない場合はチームに確認してください)。

```json
{
  "app": ここに実際のアプリID
}
```

> このファイルもリポジトリには含まれません(`.gitignore` 済み)。各自で作成してください。

---

### 2.6 実際にビルドしてみる

ここまでで、開発を始める準備が整いました。試しにビルドしてみましょう。

```bash
npm run build
```

エラーなく完了し、`dist/customize/` フォルダの中にファイルが生成されていれば成功です。これで `src/customize/` にある JavaScript/CSS を編集 → ビルドという開発の基本サイクルが回せる状態になっています。

ファイルを保存するたびに自動でビルドしたい場合は、以下のコマンドを使うと便利です(終了は `Ctrl + C`)。

```bash
npm run watch
```

---

### 2.7 (任意) 開発環境にアップロードして動作確認する

ビルドしたファイルを、実際に kintone の開発環境アプリへアップロードして反映することもできます。

```bash
npm run upload
```

> **注意**
>
> - 必ず**開発環境**(検証用の kintone 環境)に対して実行してください。本番環境への直接アップロードは禁止です。
> - Claude Code 経由で実行する場合、このコマンドは実行前に確認(ask)が入るよう安全装置がかけられています。誤って意図しないアップロードが走らないようになっています。

---

### 2.8 うまくいかないときは

| 症状                           | 確認すること                                                                                                                            |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| `npm install` でエラーが出る   | Node.js が正しくインストールされているか(`node --version`)。社内ネットワーク/プロキシの設定が必要な場合あり                             |
| `npm run check:env` で失敗する | `.env` のスペルミス、URL の末尾の `/` の有無、パスワードの誤り                                                                          |
| `npm run build` でエラーが出る | エディタでソースコードに構文エラーがないか(赤い波線が出ていないか)を確認。`npm run type-check` を実行すると詳細なエラー箇所がわかります |
| `npm run upload` で失敗する    | 接続先が正しい開発環境かどうか、`customize-manifest.json` のアプリ ID が正しいかを確認                                                  |

それでも解決しない場合は、エラーメッセージ全文を添えてチーム内で相談してください。

---

## 3. プラグイン開発

ここまでの章は、特定のアプリに紐づく「カスタマイズ」の開発でした。この章では、複数の環境・アプリに配布できる**プラグイン**の開発方法を説明します。プラグインが不要な場合は、この章は読み飛ばして問題ありません。

### 3.1 カスタマイズ開発との違い

|                  | カスタマイズ(`src/customize/`)              | プラグイン(`src/plugin/`)                                     |
| ---------------- | ------------------------------------------- | ------------------------------------------------------------- |
| 対象             | 特定の1アプリに直接反映する JS/CSS          | 複数のアプリ/環境に配布できるパッケージ(zip)                  |
| 反映方法         | `npm run upload` でアプリに直接アップロード | zip ファイルを kintone にインポートして有効化                 |
| 向いているケース | 自社の特定アプリだけをカスタマイズしたい    | 汎用的な機能を作って複数アプリ・複数kintone環境で使い回したい |

### 3.2 ディレクトリ構成

プラグインのソースは `src/plugin/` にまとまっています。

```
src/plugin/
├── manifest.json   # プラグインの名前・アイコン・設定画面などを定義するメタデータ
├── src/            # TypeScript ソース(ビルドすると js/ にまとめられる)
│   ├── desktop.ts  #   PC 画面で動く処理
│   └── config.ts   #   プラグインの設定画面の処理
├── css/            # CSS(そのままコピーされる)
├── html/           # 設定画面の HTML
└── image/          # アイコンなどの画像
```

`manifest.json` の中の `desktop.js` / `config.js` には、**ビルド後**のパス(`js/desktop.js` など)を書く点に注意してください(`src/desktop.ts` ではありません)。

### 3.3 ビルド・アップロード

```bash
npm run plugin:build          # src/plugin/ → dist/plugin.zip(署名込み)にビルド
npm run plugin:upload         # dist/plugin.zip を kintone にアップロード
npm run plugin:build:upload   # ビルド → アップロードを一括実行
```

`plugin:build` を実行すると、内部では以下の処理が順番に行われます。

1. `src/plugin/src/*.ts` を esbuild で `dist/plugin/js/*.js` にバンドル
2. `manifest.json` / `css` / `html` / `image` を `dist/plugin/` にコピー
3. 署名して `dist/plugin.zip` として1つのファイルにまとめる(パッケージ化)

> **注意**: `npm run plugin:upload` および `plugin:build:upload` も、カスタマイズのアップロードと同様に、Claude Code 経由で実行する場合は事前確認(ask)が入る安全装置がかかっています。

### 3.4 コンパイルレベル(最適化)の指定

`plugin:build` にオプションを付けると、コードの圧縮・最適化レベルを変更できます。

```bash
npm run plugin:build --opt-level=SIMPLE
```

| 値                | 内容                                                                                                                                                      |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `WHITESPACE_ONLY` | 空白・コメントを除去するだけ(安全だが軽い最適化)                                                                                                          |
| `SIMPLE`(推奨)    | ローカル変数名を短くリネームして圧縮                                                                                                                      |
| `ADVANCED`        | すべてのシンボルをリネームし、使われていないコードも除去(最も強力だが、kintone のグローバル API 呼び出しが壊れることがあるため、動作確認を入念に行うこと) |

迷った場合は、まずオプションなし(デフォルト)か `SIMPLE` を使うことをおすすめします。`ADVANCED` を使う場合は、必ず開発環境で一通りの機能を動作確認してから配布してください。

### 3.5 秘密鍵 `private.ppk` の扱い(重要)

プラグインは `src/plugin/private.ppk` という秘密鍵ファイルで署名されます。この鍵によって「同じプラグインの更新」か「別の新しいプラグイン」かが kintone 側で判定されるため、**この鍵を紛失するとプラグインの更新ができなくなります**(=新規プラグインとして登録し直すしかなくなります)。

#### 新規にプラグインを作る場合

初回の `npm run plugin:build` 実行時に自動生成され、`src/plugin/private.ppk` に保存されます。以降のビルドでは同じ鍵が使い回されるため、通常は意識する必要はありません。

#### すでにプラグインを配布していて、その鍵を持っている場合

既存の `.ppk` ファイルを、以下のパスに配置するだけで自動的に使われます。

```bash
cp /path/to/your-existing.ppk src/plugin/private.ppk
```

- ファイル名は必ず **`private.ppk`** にしてください(スクリプトがこの名前で固定的に参照しています)
- このファイルは `.gitignore` 済みで Git 管理対象外です。**別途、安全な場所(社内の共有の鍵管理場所など)にバックアップを取っておくことを強く推奨します**
- 鍵を変えてビルドすると、kintone からは「別のプラグイン」として扱われ、既存プラグインの上書き更新ができなくなるので注意してください

---

## 4. MCP連携(Claude Codeからkintoneを直接操作する)

この章は、Claude Code を使って開発している場合の発展的な内容です。Claude Code を使わない場合は読み飛ばして問題ありません。

### 4.1 MCPとは

MCP(Model Context Protocol)は、Claude Code のような AI アシスタントが外部サービス(kintone やブラウザなど)を直接操作できるようにするための仕組みです。MCP を設定すると、コマンドを自分で打たなくても、AI との自然な会話だけで kintone のレコード操作やブラウザでの動作確認ができるようになります。

### 4.2 セットアップ手順

1. サンプルファイルをコピーして `.mcp.json` を作成します。

   ```bash
   cp .mcp.example.json .mcp.json
   ```

2. 必要に応じて `.mcp.json` の内容を確認・編集します。kintone への認証情報は `.env`(2.4 節で設定したもの)から自動的に読み込まれるため、`.mcp.json` にパスワードなどを直接書く必要はありません。

> `.mcp.json` は `.gitignore` 済みのため、Git 管理対象外です。各自の PC ごとに作成してください。

### 4.3 使える MCP サーバー

| MCP サーバー                                                  | できること                                                                                                                   |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **`@kintone/mcp-server`**(公式 kintone MCP サーバー)          | 自然言語での指示で、kintone のレコードの参照・追加・更新・削除ができる                                                       |
| **`@playwright/mcp`**(Microsoft 公式 Playwright MCP サーバー) | ブラウザを自動操作し、カスタマイズ・プラグイン反映後の画面の動作確認ができる。初回実行時にブラウザが自動でダウンロードされる |

### 4.4 利用時の注意点(重要)

`@kintone/mcp-server` は**書き込み系の操作(レコードの追加・更新・削除)も行える**ため、必ず接続先が**開発環境**になっていることを確認してください。本番のテナントに接続した状態で AI に操作を任せてしまうと、意図しないデータ変更が起きるリスクがあります。`.env` に設定した `KINTONE_BASE_URL` が開発環境のものになっているか、作業前に必ず確認する習慣をつけてください。

### 4.5 対話例

MCP を設定すると、Claude Code に対して以下のような自然言語の指示で操作を依頼できます。

```
「kintone のレコード一覧画面を開いて、追加したカスタマイズが動いているか確認して」
「プラグイン設定画面のフォームに値を入力して保存できるかテストして」
「ヘッドレスでログイン画面を開いてスクリーンショットを撮って」
```

カスタマイズ・プラグイン開発そのもの(コーディングやビルド)への指示例は、`claude-tips.md` にもまとまっているので、あわせて参照してください。

---

## 5. 他に参考にした方がよいファイル

今回のマニュアルは `README.md` / `setup.md` / `mcp.md` の内容をもとに、初心者向けに手順を整理し、プラグイン開発・MCP連携についても本編(3章・4章)に含めています。あわせて以下のファイルも状況に応じて参照すると理解が深まります。

| ファイル                                      | どんなときに読むか                                                                                                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`claude-tips.md`**                          | Claude Code にどんな指示の出し方をすればよいか、対話の具体例(カスタマイズ・プラグイン開発向け)を見たい場合                                                         |
| **`CLAUDE.md`**                               | Claude Code が kintone 開発時にどんなルール(公式ドキュメントを必ず参照する、非推奨SDKを使わない等)に従っているか知りたい場合。通常、自分で編集する必要はありません |
| **`settings.json`**(`.claude/settings.json`)  | どのコマンドが「確認なしで実行可」「確認が必要」「実行禁止」に設定されているか確認したい場合。AI に安全に作業を任せるためのガードレール設定です                    |
| **ルート `README.md` のディレクトリ構成**の項 | プロジェクト全体のフォルダ構成を俯瞰的に把握したい場合                                                                                                             |
| **`README.md` のコマンド一覧**                | 今回のマニュアルで紹介しきれていない細かいコマンド(`lint` / `format` / `gen:types` の詳細オプションなど)を一覧で確認したい場合                                     |

これらは今回のマニュアルの範囲には必須ではありませんが、Claude Code を使った開発を本格的に進める段階で目を通しておくとスムーズです。
