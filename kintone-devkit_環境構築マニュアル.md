# kintone-devkit 環境構築マニュアル

対象読者: Git / Node.js にまだ慣れていない社内メンバー
ゴール: このマニュアル通りに進めれば、自分の PC で kintone カスタマイズのビルドまでできる状態になります。

---

## 目次

1. [kintone-devkit とは何か](#1-kintone-devkit-とは何か)
2. [環境構築手順](#2-環境構築手順)
   - 2.1 [事前に必要なソフトをインストールする](#21-事前に必要なソフトをインストールする)
   - 2.2 [リポジトリを手元に取得する](#22-リポジトリを手元に取得する)
   - 2.3 [パッケージをインストールする](#23-パッケージをインストールする)
   - 2.4 [kintone の認証情報を設定する](#24-kintone-の認証情報を設定する)
   - 2.5 [アプリの設定ファイルを作る](#25-アプリの設定ファイルを作る)
   - 2.6 [実際にビルドしてみる](#26-実際にビルドしてみる)
   - 2.7 [(任意) 開発環境にアップロードして動作確認する](#27-任意-開発環境にアップロードして動作確認する)
   - 2.8 [うまくいかないときは](#28-うまくいかないときは)
3. [他に参考にした方がよいファイル](#3-他に参考にした方がよいファイル)

---

## 1. kintone-devkit とは何か

**kintone-devkit** は、kintone のカスタマイズ(JavaScript/CSS)やプラグインを開発するための社内テンプレートです。難しいセットアップを毎回自分で組む必要がないよう、あらかじめ「よく使う仕組み」が一式そろっています。

### できること

| 機能 | ひとことで言うと |
|---|---|
| カスタマイズ開発 | アプリごとの JS/CSS を書いて、kintone に自動アップロードできる |
| プラグイン開発 | 配布用のプラグイン(zip)を作成・署名・アップロードできる |
| 型定義の自動生成 | kintone のフィールド情報から TypeScript の型を自動で作ってくれる(入力ミスを防げる) |
| AI(Claude Code)連携 | ソースコードの規約や、うっかり事故を防ぐガードレールがあらかじめ用意されている |

### 覚えておきたい用語(初心者向け)

- **Git / リポジトリ**: ソースコードの変更履歴を管理する仕組み。「リポジトリ」はコード一式が入ったフォルダのようなものです。
- **clone(クローン)**: リポジトリをインターネット上(GitHub など)から自分の PC にコピーしてくること。
- **Node.js / npm**: JavaScript のプログラムを実行するための土台(Node.js)と、それに付属するパッケージ管理ツール(npm)。`npm install` で必要な部品を一括ダウンロードします。
- **ビルド(build)**: 自分が書いたソースコードを、kintone が読み込める形式のファイルに変換する作業。
- **.env ファイル**: パスワードなどの秘密情報を書いておくための設定ファイル。Git の管理対象から除外されており、誤って社外に公開されない仕組みになっています。

このマニュアルでは、上記の用語がわからなくても迷わず進められるよう、手順を1つずつ説明します。

---

## 2. 環境構築手順

### 2.1 事前に必要なソフトをインストールする

作業を始める前に、以下の3つを PC にインストールしてください。

| ソフト | 用途 | 入手先 |
|---|---|---|
| **Git** | リポジトリを取得・管理するために必要 | https://git-scm.com/downloads |
| **Node.js**(LTS版) | ビルドやコマンド実行に必要 | https://nodejs.org/ (「LTS」と書かれている方を選ぶ) |
| **Visual Studio Code**(推奨) | ソースコードを編集するエディタ | https://code.visualstudio.com/ |

インストール後、ターミナル(Windows は「コマンドプロンプト」または「PowerShell」、Mac は「ターミナル」)を開いて、以下のコマンドでインストールできているか確認します。

```bash
git --version
node --version
npm --version
```

それぞれバージョン番号が表示されれば OK です。何も表示されない/エラーになる場合は、インストールがうまくいっていないので、PC を再起動してから再度確認してください。

> **補足**: Claude Code を使って AI と対話しながら開発したい場合は、別途 Claude Code のインストールが必要です。導入手順は社内の別ドキュメントを参照するか、担当者に確認してください。このマニュアルでは Claude Code を使わない場合でも最後までビルドできる手順を説明します。

---

### 2.2 リポジトリを手元に取得する

作業用のフォルダを決め、ターミナルでそのフォルダに移動してから、以下を実行します(URL は実際のリポジトリの URL に置き換えてください)。

```bash
git clone <リポジトリのURL>
cd <クローンされたフォルダ名>
```

これで、README.md や CLAUDE.md などのファイルが入ったフォルダが手元にできます。

---

### 2.3 パッケージをインストールする

リポジトリのフォルダの中で、以下を実行します。

```bash
npm install
```

`package.json` に書かれた必要な部品(esbuild, TypeScript など)が自動でダウンロードされます。数十秒〜数分かかることがあります。エラーが出ずに完了すれば成功です。

---

### 2.4 kintone の認証情報を設定する

ビルドしたカスタマイズを kintone にアップロードしたり、フィールド情報を取得したりするために、kintone への接続情報(URL・ユーザー名・パスワードなど)を設定します。

#### 手順

1. リポジトリ直下にある `.env.example` を `.env` という名前でコピーします。

   ```bash
   cp .env.example .env
   ```

   (Windows でコマンドがうまく動かない場合は、エクスプローラーで `.env.example` をコピー&貼り付けし、ファイル名を `.env` に変更してください。)

2. `.env` を VS Code などのエディタで開き、以下のように自分の kintone 環境の情報を書き込みます。

   ```env
   KINTONE_BASE_URL=https://your-subdomain.cybozu.com
   KINTONE_USERNAME=your-username
   KINTONE_PASSWORD=your-password
   ```

   値は担当者・チームリーダーに確認してください(開発環境用のアカウントを使うのが基本です)。

   > **重要**: `.env` は Git の管理対象外(`.gitignore` 設定済み)になっているため、ここに書いた内容が誤って社外に公開されることはありません。逆に言うと、このファイルは各自の PC ごとに個別に作成する必要があります(リポジトリに最初から入っているわけではありません)。

   > 個人検証レベルであれば、上記のように `.env` に直接書くだけで問題ありません。認証情報をリポジトリのフォルダから完全に切り離しておきたい場合は、次の「認証情報をリポジトリの外に置く方法」を参照してください。

3. 正しく設定できたか確認します。

   ```bash
   npm run check:env
   ```

   `✓ 認証情報の読み込みに成功しました` と表示されれば成功です。エラーが出る場合は `.env` の内容(スペルミスや余分な空白など)を見直してください。

#### 認証情報をリポジトリの外に置く方法(推奨)

`.env` はすでに Git の管理対象外になっているため、直接パスワードを書いても社外に流出する心配は基本的にありません。ただし、「PC 内であってもリポジトリのフォルダの中にパスワードの実物を置きたくない」という場合は、以下の方法で認証情報そのものをリポジトリの外に隔離できます。

1. PC 内の、リポジトリとは別の場所に秘密情報用のフォルダを作り、そこに実際の認証情報を書いたファイルを作成します。フォルダ名・ファイル名は自由ですが、例として以下のような場所を使います。

   - Windows: `C:\Users\<自分のユーザー名>\secrets\kintone-<プロジェクト名>.env`
   - Mac: `~/secrets/kintone-<プロジェクト名>.env`

   このファイルの中身は、通常の `.env` と同じ形式で書きます。

   ```env
   KINTONE_BASE_URL=https://your-subdomain.cybozu.com
   KINTONE_USERNAME=your-username
   KINTONE_PASSWORD=your-password
   ```

2. リポジトリ直下の `.env`(`.env.example` をコピーして作ったもの)には、値を直接書く代わりに、上記1で作ったファイルへの「パス(ファイルの場所)」だけを1行で書きます。

   ```env
   # <リポジトリのフォルダ>/.env
   ENV_FILE_REDIRECT=C:\Users\<自分のユーザー名>\secrets\kintone-<プロジェクト名>.env
   ```

   Mac の場合は例えば以下のようになります。

   ```env
   ENV_FILE_REDIRECT=/Users/<自分のユーザー名>/secrets/kintone-<プロジェクト名>.env
   ```

   > 相対パス(`../secrets/...` のような書き方)を使う場合は、`.env` が置かれているフォルダを基準に解決されます。慣れないうちは、上記のようにフルパス(絶対パス)で書く方が確実です。

3. この状態で `npm run check:env` を実行すると、リポジトリ内の `.env` ではなく、リダイレクト先の外部ファイルが読み込まれ、「ソース」の欄に外部ファイルのパスが表示されます。これで正しくリダイレクトされていることが確認できます。

   ```bash
   npm run check:env
   ```

> **この方法のメリット**: リポジトリのフォルダを誰かと共有したり、フォルダごと zip にして送ったりしても、パスワードの実物が含まれない状態になります。チームで統一ルールがある場合は、そちらの手順に従ってください。

---

### 2.5 アプリの設定ファイルを作る

カスタマイズをどの kintone アプリに反映するかを指定するファイルを作成します。

```bash
cp customize-manifest.example.json customize-manifest.json
```

作成した `customize-manifest.json` をエディタで開き、`"app"` の値を、実際に対象とする kintone アプリの ID に書き換えます(アプリ ID は kintone のアプリ画面の URL に含まれる数字です。わからない場合はチームに確認してください)。

```json
{
  "app": ここに実際のアプリID
}
```

> このファイルもリポジトリには含まれません(`.gitignore` 済み)。各自で作成してください。

---

### 2.6 実際にビルドしてみる

ここまでで、開発を始める準備が整いました。試しにビルドしてみましょう。

```bash
npm run build
```

エラーなく完了し、`dist/customize/` フォルダの中にファイルが生成されていれば成功です。これで `src/customize/` にある JavaScript/CSS を編集 → ビルドという開発の基本サイクルが回せる状態になっています。

ファイルを保存するたびに自動でビルドしたい場合は、以下のコマンドを使うと便利です(終了は `Ctrl + C`)。

```bash
npm run watch
```

---

### 2.7 (任意) 開発環境にアップロードして動作確認する

ビルドしたファイルを、実際に kintone の開発環境アプリへアップロードして反映することもできます。

```bash
npm run upload
```

> **注意**
> - 必ず**開発環境**(検証用の kintone 環境)に対して実行してください。本番環境への直接アップロードは禁止です。
> - Claude Code 経由で実行する場合、このコマンドは実行前に確認(ask)が入るよう安全装置がかけられています。誤って意図しないアップロードが走らないようになっています。

---

### 2.8 うまくいかないときは

| 症状 | 確認すること |
|---|---|
| `npm install` でエラーが出る | Node.js が正しくインストールされているか(`node --version`)。社内ネットワーク/プロキシの設定が必要な場合あり |
| `npm run check:env` で失敗する | `.env` のスペルミス、URL の末尾の `/` の有無、パスワードの誤り |
| `npm run build` でエラーが出る | エディタでソースコードに構文エラーがないか(赤い波線が出ていないか)を確認。`npm run type-check` を実行すると詳細なエラー箇所がわかります |
| `npm run upload` で失敗する | 接続先が正しい開発環境かどうか、`customize-manifest.json` のアプリ ID が正しいかを確認 |

それでも解決しない場合は、エラーメッセージ全文を添えてチーム内で相談してください。

---

## 3. 他に参考にした方がよいファイル

今回のマニュアルは主に `README.md` と `setup.md` の内容をもとに、初心者向けに手順を整理したものです。あわせて以下のファイルも状況に応じて参照すると理解が深まります。

| ファイル | どんなときに読むか |
|---|---|
| **`mcp.md`** | Claude Code から自然言語で kintone のレコード操作やブラウザ動作確認をさせたい場合(AI 活用を発展させたいメンバー向け) |
| **`claude-tips.md`** | Claude Code にどんな指示の出し方をすればよいか、具体例を見たい場合 |
| **`CLAUDE.md`** | Claude Code が kintone 開発時にどんなルール(公式ドキュメントを必ず参照する、非推奨SDKを使わない等)に従っているか知りたい場合。通常、自分で編集する必要はありません |
| **`settings.json`**(`.claude/settings.json`) | どのコマンドが「確認なしで実行可」「確認が必要」「実行禁止」に設定されているか確認したい場合。AI に安全に作業を任せるためのガードレール設定です |
| **ルート `README.md` のディレクトリ構成**の項 | プロジェクト全体のフォルダ構成を俯瞰的に把握したい場合 |
| **`docs/setup.md` のプラグイン開発の章** | 今回のマニュアルではカスタマイズ開発を中心に説明したため、プラグイン開発(配布用パッケージの作成)に進む場合は改めてこちらを参照してください |

これらは今回のマニュアルの範囲(=まず開発・ビルドができる状態にする)には必須ではありませんが、Claude Code を使った開発を本格的に進める段階で目を通しておくとスムーズです。
